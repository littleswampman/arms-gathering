import React, { FC } from "react";
import { Layout } from "../layout/Layout";

export const GameOver: FC = () => {
    return (
        <Layout>
            <div>GameOver</div>
        </Layout>
    );
};
