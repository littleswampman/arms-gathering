import React, { FC } from "react";
import { Layout } from "../layout/Layout";

export const Battle: FC = () => {
    return (
        <Layout>
            <div>Battle</div>
        </Layout>
    );
};
