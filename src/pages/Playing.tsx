import React, { FC } from "react";
import { Layout } from "../layout/Layout";

export const Playing: FC = () => {
    return (
        <Layout>
            <div>Playing</div>
        </Layout>
    );
};
